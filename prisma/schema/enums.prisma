enum Role {
    CUSTOMER
    ADMIN
    PROVIDER
}

enum RentalStatus {
    PENDING
    CONFIRMED
    PICKED_UP
    RETURNED
    CANCELLED
}

enum PaymentMethod {
  STRIPE
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}